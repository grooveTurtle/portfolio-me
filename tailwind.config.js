/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{html,js,svelte,ts}'], // Svelte 파일들을 포함
  theme: {
    extend: {}, // 필요한 경우 커스터마이징
  },
  plugins: [], // 추가 플러그인을 사용하고 싶다면 여기에 추가
}